---
sidebar_position: 5
id: alert-fourthwall-newsletter-subscribed
title: Newsletter Subscribed
description: subscribed to the newsletter
---

# Newsletter Subscribed

- **Alert key:** `fourthwall-newsletterSubscribed`
- **Source:** Fourthwall
- **Event list label:** Newsletter Subscribed

## Default message template

```text
{{email}} subscribed to the newsletter
```

## Template variables

Use any of these in alert text, TTS, or chat templates. The same names appear under `data.extraSettings.*` when you subscribe via `Overlay.on('alert')`.

| Template variable | Overlay payload field | Description |
| --- | --- | --- |
| `{{email}}` | `data.extraSettings.email` | Email of user |
| `{{raw}}` | `data.extraSettings.raw` | The raw data that comes in through the platform for the alert. This is useful for custom code when data is needed that we don't provide by default. |

## Example payloads

Each scenario below mirrors what Lumia emits in `Overlay.on('alert', (data) => …)`.

### Subscribed

```json
{
  "alert": "fourthwall-newsletterSubscribed",
  "dynamic": {
    "value": 1
  },
  "extraSettings": {
    "email": "test@email.com",
    "raw": ""
  }
}
```

## Variation conditions

Dimensions you can branch on when configuring alert variations:

- `RANDOM`
- `EQUAL_VARIABLE`

## Related

- [Alert Explorer](/docs/display-variables#alert-explorer) - live preview of every alert
- [Overlay Type Definitions](/docs/custom-overlays/types#alertevent) - full `AlertEvent` TypeScript type
