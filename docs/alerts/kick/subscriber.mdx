---
sidebar_position: 12
id: alert-kick-subscriber
title: Subscriber
description: "subscribed with a {{tier}} sub"
---

# Subscriber

- **Alert key:** `kick-subscriber`
- **Source:** Kick
- **Event list label:** `{{tier}}` Sub

## Default message template

```text
{{username}} just subscribed with a {{tier}} sub!
```

## Template variables

Use any of these in alert text, TTS, or chat templates. The same names appear under `data.extraSettings.*` when you subscribe via `Overlay.on('alert')`.

| Template variable | Overlay payload field | Description |
| --- | --- | --- |
| `{{username}}` | `data.extraSettings.username` | The username of the sender |
| `{{avatar}}` | `data.extraSettings.avatar` | The url of the avatar of the sender for a message |
| `{{tier}}` | `data.extraSettings.tier` | Tier gift |
| `{{recipient}}` | `data.extraSettings.recipient` | User being gifted the sub |
| `{{subMonths}}` | `data.extraSettings.subMonths` | How many months the user has been subbed for |
| `{{streakMonths}}` | `data.extraSettings.streakMonths` | How many months the user has been subbed for in a row |
| `{{message}}` | `data.extraSettings.message` | The remaining message after removing the command from the raw message |
| `{{subPlan}}` | `data.extraSettings.subPlan` | Subs plan value (1000, 2000, 3000) |
| `{{subPlanName}}` | `data.extraSettings.subPlanName` | Subs plan value name (Tier 1, Tier 2, Tier 3) |
| `{{amount}}` | `data.extraSettings.amount` | The amount sent |

## Example payloads

Each scenario below mirrors what Lumia emits in `Overlay.on('alert', (data) => …)`.

### Tier 1 Sub

```json
{
  "alert": "kick-subscriber",
  "dynamic": {
    "value": "1",
    "amount": 1
  },
  "extraSettings": {
    "username": "lumiastream",
    "avatar": "https://static-cdn.jtvnw.net/jtv_user_pictures/2b1fa336-f9b2-42cf-bd2c-98675da74982-profile_image-70x70.png",
    "message": "Great Stream",
    "subMonths": 1,
    "amount": 1,
    "tier": "Tier 1",
    "subPlan": "1",
    "subPlanName": "My Day ones",
    "recipient": "",
    "streakMonths": ""
  }
}
```

## Variation conditions

Dimensions you can branch on when configuring alert variations:

- `RANDOM`
- `SUBSCRIBED_MONTHS_EQUAL` - Months Subscribed is equal to
- `SUBSCRIBED_MONTHS_GREATER` - Months Subscribed is greater than or equal to
- `EQUAL_VARIABLE`

## Related

- [Alert Explorer](/docs/display-variables#alert-explorer) - live preview of every alert
- [Overlay Type Definitions](/docs/custom-overlays/types#alertevent) - full `AlertEvent` TypeScript type
